{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open Moster Burger in Browser",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:4173",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Run Moster Burger (Python server)"
    }
  ]
}
